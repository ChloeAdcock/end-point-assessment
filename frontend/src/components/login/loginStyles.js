import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  container: {
    textAlign: 'center',
    marginTop: '5%',
  },
  button: {
      marginTop: '5%',
      marginBottom: '10%'
  },
  typography: {
      fontSize: 25,
  }
});